---
title: Speakers
sidebar_label: Speakers
---

import { Card } from "./components.js";

export const speakers = [
  {
    name: "Philippe Schwaller",
    link: "https://pschwllr.github.io/",
    img: "https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTFs9Gga1TInCkAaUiIrNLgrBrGHq4MQXBvTl07TCXUVw&s",
    organization: "EPFL",
    talkTitle: ""
  },
];


## Speakers

<div className="container">
  <div className="row">
    {speakers
      .sort(function (a, b) {
        return a.name.localeCompare(b.name);
      })
      .map((props, idx) => (
        <Card key={idx} {...props} />
      ))}
  </div>
</div>